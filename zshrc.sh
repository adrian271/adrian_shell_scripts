ALIAS_FILE="$HOME/bin/zsh_aliases.sh"
[ -f "$ALIAS_FILE" ] && source "$ALIAS_FILE"

HIST_FILE="$HOME/bin/zsh_hist.sh"
[ -f "$HIST_FILE" ] && source "$HIST_FILE"

TEEP_FILE="$HOME/bin/zsh_teep.sh"
[ -f "$TEEP_FILE" ] && source "$TEEP_FILE"